import { IMAGE_URL } from "./tmdb";

export const posterSizes = {
  original: IMAGE_URL + "/original",
  w500: IMAGE_URL + "/w500",
};

export const logoSizes = {
  original: IMAGE_URL + "/original",
  w185: IMAGE_URL + "/w185",
};

export const profilePathSizes = {
  original: IMAGE_URL + "/original",
  h632: IMAGE_URL + "/h632",
};
